apiVersion: consul.hashicorp.com/v1alpha1
kind: ServiceRouter
metadata:
  name: hashicups
spec:
  routes:
    - match:
        http:
          pathExact: '/api'
      destination:
        service: public-api
    - match:
        http:
          pathExact: '/'
      destination:
        service: frontend
