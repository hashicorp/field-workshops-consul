# Copyright (c) HashiCorp, Inc.
# SPDX-License-Identifier: MPL-2.0
name: hashicorp/field-workshops-consul/deploy-service-discovery-with-consul
on:
  workflow_dispatch:
  push:
    branches:
      - ${{ vars.DEPLOY_BRANCH }}
    paths:
      - 'instruqt-tracks/service-discovery-with-consul/**'
permissions:
  contents: read
jobs:
  deploy-track:
    uses: ./.github/workflows/instruqt-track-deploy.yml
    with:
      working_directory: "instruqt-tracks/service-discovery-with-consul"
      INSTRUQT_CLI_URI: ${{ vars.INSTRUQT_CLI_URI }}
    secrets:
      INSTRUQT_TOKEN: ${{ secrets.INSTRUQT_TOKEN }}
